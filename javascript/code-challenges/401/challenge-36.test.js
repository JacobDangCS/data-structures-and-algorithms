const Graph = require('./challenge-36');

describe('Tests the graph structure', () => {

  const graph = new Graph();

  it('Tests for ', () => {
    graph.addVertex(1);
    expect(graph).toBeTruthy();
  });

  it('Tests for ', () => {
    graph.addVertex(1);
    expect(graph).toBeTruthy();
  });

  it('Tests for ', () => {
    graph.addVertex(1);
    expect(graph).toBeTruthy();
  });

});
